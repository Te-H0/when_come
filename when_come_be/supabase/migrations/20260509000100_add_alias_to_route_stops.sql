-- ─────────────────────────────────────────────────────────
-- T2. route_stops.alias 컬럼 추가
-- SDD §2.3
-- 2026-05-09
-- ─────────────────────────────────────────────────────────

-- nullable. 빈 문자열 저장 = NULL로 정규화 (BE 책임).
-- 기존 row는 NULL로 자동 채워짐 — 데이터 영향 없음.
alter table route_stops add column alias text;
